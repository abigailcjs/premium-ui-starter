---
description: Start a guided, step-by-step build where Claude proposes a few options at each decision and you pick.
---

Start the **guided-build** walkthrough for this repo. Invoke the `guided-build`
skill and follow it: confirm the build mode from `.claude/build-preferences.json`,
then walk the user through building — proposing a few clear options at each step
and waiting for their pick before you build. Begin at step 1 ("What are we
building?"). Keep it warm, plain-spoken, and one decision at a time.
